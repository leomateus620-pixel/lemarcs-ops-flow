import { GlassCard } from "./GlassCard";
export function ReportCard({ title, data }: { title: string; data: { nome: string; os?: number; horas?: number }[] }) { const max = Math.max(...data.map((d) => d.os ?? d.horas ?? 1)); return <GlassCard className="space-y-3 p-4"><h3 className="section-title">{title}</h3>{data.map((d) => { const v = d.os ?? d.horas ?? 0; return <div key={d.nome}><div className="flex justify-between text-xs"><span className="font-bold text-foreground">{d.nome}</span><span className="text-muted-foreground">{v}</span></div><div className="mt-1 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-gradient-to-r from-primary to-orange-glow" style={{ width: `${(v / max) * 100}%` }} /></div></div>; })}</GlassCard>; }
